# requirements.txt
# MultiVerse Backend — Pinned Dependencies
#
# INSTALL ORDER IS CRITICAL:
# Step 1 — Install PyTorch CPU-only first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# Step 2 — Then install this file:
#   pip install -r requirements.txt
#
# ⚠️  ctranslate2 is NOT supported on Python 3.13. The system will
#     automatically fallback to openai-whisper.
# ⚠️  Using numpy>=2.1.0 for Python 3.13 compatibility.
# ⚠️  Using faiss-cpu>=1.9.0 for Python 3.13 compatibility.

faster-whisper==1.0.3
pyaudio==0.2.14
numpy>=2.1.0
sentence-transformers==2.7.0
faiss-cpu>=1.9.0
word2number==1.1
rapidfuzz==3.6.1
librosa==0.10.2
tqdm==4.66.4
whisper
